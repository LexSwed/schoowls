# Migration `20200708222605`

This migration has been generated by Alexander Swed at 7/8/2020, 10:26:05 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "User" (
"disabled" BOOLEAN  DEFAULT false,
"email" TEXT NOT NULL,
"id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
"issuer" TEXT NOT NULL,
"lastActivityAt" DATE ,
"lastLoginAt" DATE ,
"name" TEXT ,
"registeredAt" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP)

CREATE TABLE "UserDetails" (
"alias" TEXT ,
"avatarUrl" TEXT ,
"id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
"phoneNumber" TEXT ,
"timeZone" TEXT ,
"userId" INTEGER NOT NULL,FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE UNIQUE INDEX "User.issuer" ON "User"("issuer")

CREATE UNIQUE INDEX "User.email" ON "User"("email")

CREATE UNIQUE INDEX "UserDetails_userId" ON "UserDetails"("userId")

PRAGMA foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200705184407..20200708222605
--- datamodel.dml
+++ datamodel.dml
@@ -1,23 +1,31 @@
 datasource db {
     provider = "sqlite"
-    url = "***"
+    url = "***"
 }
 generator client {
     provider = "prisma-client-js"
 }
 // internal info
 model User {
-    id             Int       @default(autoincrement()) @id
+    id             Int         @default(autoincrement()) @id
     name           String?
-    alias          String?
-    phoneNumber    String?
-    avatarUrl      String?
-    issuer         String    @unique
-    email          String    @unique
-    disabled       Boolean?  @default(false)
-    registeredAt   DateTime  @default(now())
-    lastLoginAt    DateTime?
-    lastActivityAt DateTime?
+    details        UserDetails
+    issuer         String      @unique
+    email          String      @unique
+    disabled       Boolean?    @default(false)
+    registeredAt   DateTime    @default(now()) // ISO Date
+    lastLoginAt    DateTime? // ISO Date
+    lastActivityAt DateTime? // ISO Date
 }
+
+model UserDetails {
+    id          Int     @default(autoincrement()) @id
+    user        User    @relation(fields: [userId], references: [id])
+    userId      Int
+    alias       String?
+    phoneNumber String?
+    avatarUrl   String?
+    timeZone    String?
+}
```


