# Migration `20200704135129-detail`

This migration has been generated by Alexander Swed at 7/4/2020, 1:51:29 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Account" (
"active" BOOLEAN   ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"lastLoggedInAt" DATE   ,"registeredAt" DATE NOT NULL  )

INSERT INTO "quaint"."new_Account" ("active", "id", "lastLoggedInAt", "registeredAt") SELECT "active", "id", "lastLoggedInAt", "registeredAt" FROM "quaint"."Account"

PRAGMA foreign_keys=off;
DROP TABLE "quaint"."Account";;
PRAGMA foreign_keys=on

ALTER TABLE "quaint"."new_Account" RENAME TO "Account";

CREATE TABLE "quaint"."new_User" (
"accountId" INTEGER NOT NULL  ,"alias" TEXT   ,"email" TEXT NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"name" TEXT   ,"phoneNumber" TEXT   ,FOREIGN KEY ("accountId") REFERENCES "Account"("id") ON DELETE CASCADE ON UPDATE CASCADE)

INSERT INTO "quaint"."new_User" ("accountId", "id") SELECT "accountId", "id" FROM "quaint"."User"

PRAGMA foreign_keys=off;
DROP TABLE "quaint"."User";;
PRAGMA foreign_keys=on

ALTER TABLE "quaint"."new_User" RENAME TO "User";

CREATE UNIQUE INDEX "quaint"."User_accountId" ON "User"("accountId")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200704134721-user..20200704135129-detail
--- datamodel.dml
+++ datamodel.dml
@@ -1,22 +1,27 @@
 datasource db {
     provider = "sqlite"
-    url = "***"
+    url = "***"
 }
 generator client {
     provider = "prisma-client-js"
 }
+// internal info
 model Account {
-    id             Int      @default(autoincrement()) @id
+    id             Int       @default(autoincrement()) @id
     user           User
-    active         Boolean
+    active         Boolean?
     registeredAt   DateTime
-    lastLoggedInAt DateTime
+    lastLoggedInAt DateTime?
 }
 model User {
-    id        Int     @default(autoincrement()) @id
-    account   Account @relation(fields: [accountId], references: [id])
-    accountId Int
+    id          Int     @default(autoincrement()) @id
+    account     Account @relation(fields: [accountId], references: [id])
+    accountId   Int
+    email       String
+    name        String?
+    alias       String?
+    phoneNumber String?
 }
```


