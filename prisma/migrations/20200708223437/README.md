# Migration `20200708223437`

This migration has been generated by Alexander Swed at 7/8/2020, 10:34:37 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

ALTER TABLE "UserDetails" ADD COLUMN "name" TEXT ;

CREATE TABLE "new_User" (
"disabled" BOOLEAN  DEFAULT false,
"email" TEXT NOT NULL,
"id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
"issuer" TEXT NOT NULL,
"lastActivityAt" DATE ,
"lastLoginAt" DATE ,
"registeredAt" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP)

INSERT INTO "new_User" ("disabled", "email", "id", "issuer", "lastActivityAt", "lastLoginAt", "registeredAt") SELECT "disabled", "email", "id", "issuer", "lastActivityAt", "lastLoginAt", "registeredAt" FROM "User"

PRAGMA foreign_keys=off;
DROP TABLE "User";;
PRAGMA foreign_keys=on

ALTER TABLE "new_User" RENAME TO "User";

CREATE UNIQUE INDEX "User.issuer" ON "User"("issuer")

CREATE UNIQUE INDEX "User.email" ON "User"("email")

PRAGMA foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200708222605..20200708223437
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
     provider = "sqlite"
-    url = "***"
+    url = "***"
 }
 generator client {
     provider = "prisma-client-js"
@@ -9,9 +9,8 @@
 // internal info
 model User {
     id             Int         @default(autoincrement()) @id
-    name           String?
     details        UserDetails
     issuer         String      @unique
     email          String      @unique
     disabled       Boolean?    @default(false)
@@ -23,8 +22,9 @@
 model UserDetails {
     id          Int     @default(autoincrement()) @id
     user        User    @relation(fields: [userId], references: [id])
     userId      Int
+    name        String?
     alias       String?
     phoneNumber String?
     avatarUrl   String?
     timeZone    String?
```


