# Migration `20200705125553`

This migration has been generated by Alexander Swed at 7/5/2020, 12:55:53 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."Token" (
"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT)

CREATE TABLE "quaint"."new_User" (
"detailsId" INTEGER NOT NULL  ,"disabled" BOOLEAN  DEFAULT false ,"email" TEXT NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"issuer" TEXT NOT NULL  ,"lastActivityAt" DATE   ,"lastLoginAt" DATE   ,"registeredAt" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP ,FOREIGN KEY ("detailsId") REFERENCES "UserDetails"("id") ON DELETE CASCADE ON UPDATE CASCADE)

INSERT INTO "quaint"."new_User" ("detailsId", "disabled", "email", "id", "lastActivityAt", "registeredAt") SELECT "detailsId", "disabled", "email", "id", "lastActivityAt", "registeredAt" FROM "quaint"."User"

PRAGMA foreign_keys=off;
DROP TABLE "quaint"."User";;
PRAGMA foreign_keys=on

ALTER TABLE "quaint"."new_User" RENAME TO "User";

CREATE UNIQUE INDEX "quaint"."User_detailsId" ON "User"("detailsId")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200704165044..20200705125553
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
     provider = "sqlite"
-    url = "***"
+    url = "***"
 }
 generator client {
     provider = "prisma-client-js"
@@ -12,11 +12,12 @@
     id             Int         @default(autoincrement()) @id
     detailsId      Int
     details        UserDetails @relation(fields: [detailsId], references: [id])
     email          String
-    emailConfirmed Boolean     @default(false)
+    issuer         String
     disabled       Boolean?    @default(false)
     registeredAt   DateTime    @default(now())
+    lastLoginAt    DateTime?
     lastActivityAt DateTime?
 }
 model UserDetails {
@@ -24,6 +25,11 @@
     name        String?
     alias       String?
     phoneNumber String?
     avatarUrl   String?
-    User        User[]
+    User        User
 }
+
+model Token {
+    id Int @default(autoincrement()) @id
+
+}
```


